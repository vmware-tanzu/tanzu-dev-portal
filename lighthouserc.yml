ci:
  collect: {}

  upload:
    target: temporary-public-storage

  assert: {}
    # preset: lighthouse:no-pwa
    # assertions:
    #   categories:acessibility: [ warn, { minScore: 0.80 } ]
    #   categories:best-practices: [ warn, { minScore: 0.65 } ]
    #   categories:performance: [ warn, { minScore: 0.25 } ]
    #   categories:seo: [ warn, { minScore: 0.65 } ]
