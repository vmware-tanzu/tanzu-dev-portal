ci:
  collect:
    # spins up a local server that serves the files
    # in this directory
    staticDistDir: './public/developer'

  upload:
    target: temporary-public-storage

  assert:
    assertions:
      categories:performance: [ warn, {minScore: 0.75}]
      categories:acessibility: error
