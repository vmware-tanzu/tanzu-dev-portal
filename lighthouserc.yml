ci:
  collect: {}

  upload:
    target: temporary-public-storage

  assert:
    preset: lighthouse:no-pwa
    assertions:
      categories:performance: [ warn, {minScore: 0.75}]
      categories:acessibility: error

