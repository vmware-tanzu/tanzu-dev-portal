name: Check and Deploy to Staging
on:
  push:
    branches-ignore:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/actions/spell-check
        name: Spell Check
