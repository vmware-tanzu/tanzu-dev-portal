# Generate a Netlify _redirects file so that aliases redirect via a 301 instead of a meta refresh

{{ printf "\n" }}

{{- range $p := .Site.Pages -}}
    {{- range .Aliases -}}
        {{- printf "%s %s\n" . $p.RelPermalink -}}
    {{- end -}}
{{- end -}}
